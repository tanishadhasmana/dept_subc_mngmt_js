<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Logout</title>
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-red-100 via-pink-100 to-purple-100 min-h-screen flex items-center justify-center">

  <div class="text-center p-8 bg-white rounded-xl shadow-lg">
    <% if (name) { %>
      <h1 class="text-3xl font-bold text-red-600 mb-4">You are successfully logged out, <%= name %>!</h1>
      <h4 class="text-gray-700 mb-4">If you want to login again:</h4>
      <form action="/login" method="get">
        <button type="submit" class="px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Login</button>
      </form>
    <% } else { %>
      <h1 class="text-2xl font-semibold text-gray-800">You are not logged in. Please <a href="/login" class="text-red-500 underline">login</a>.</h1>
    <% } %>
  </div>

</body>
</html>



<!-- this is without tailwind -->
 <%#
<!DOCTYPE html>
<html>
<head>
  <title>Logout</title>
</head>
<body>
  <% if (name) { %>
    <-- the name is fetching from cookies through the controller -->
    <h1>You are successfully logged out
        <%= name %>!</h1> 
    <h4>If want to login again - </h4>
    <form action="/login" method="get">
      <button type="submit">Login</button>
    </form>
  <% } else { %>
    <h1>You are not logged in. Please <a href="/login">login</a>.</h1>
  <% } %>
</body>
</html>
%>
